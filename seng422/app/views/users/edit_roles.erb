<h1>Changing <%= @user.username %>'s roles</h1>

<%= render partial: "errors" %>

<%= form_for @user, :url => update_roles_user_path(@user), :method => :put do |f| %>
	<%= render partial: "set_roles" %>

	<p>
		<%= f.submit %>
	</p>
<% end %>

<%= link_to "Back", users_path %>
