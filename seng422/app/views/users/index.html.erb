<h1>All users</h1>

<table>
	<tr>
		<th>Name</th>
	</tr>
	<% @users.each do |user| %>
		<tr>
			<td><%= user.username %></td>
		</tr>
	<% end %>
</table>

<%= link_to 'New user', new_user_path %><br/>

<%= link_to "Home",  "/" %>
