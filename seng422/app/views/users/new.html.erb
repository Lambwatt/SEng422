<h1>New user</h1>

<%= render partial: "errors" %>

<%= form_for :user, url: users_path do |f| %>
	<%= render partial: "edit_info", :locals => {:f => f} %>
<% end %>

<%= link_to "Back", users_path %>
